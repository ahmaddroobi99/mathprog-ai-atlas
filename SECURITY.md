# Security

This repository is documentation, catalogs, and a tiny example. It does not ship a service.

Do not open issues that include API keys, solver licenses, or private instance data. If a future notebook adds credentials, keep them in environment variables and never commit `.env`.
